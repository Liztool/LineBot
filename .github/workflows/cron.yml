name: LINE Cron Job

on:
  schedule:
    - cron: "15 * * * *"

  workflow_dispatch:  # 讓你可以手動測試

jobs:
  call-api:
    runs-on: ubuntu-latest

    steps:
      - name: Call Render Cron
        run: |
          curl -X GET "https://linebot-rehab.onrender.com/cold"
